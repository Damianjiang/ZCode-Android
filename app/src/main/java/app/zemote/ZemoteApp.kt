package app.zemote

import android.app.Application

/**
 * Application class for Zemote.
 * Currently empty; kept for future initialization hooks (e.g., analytics, crash reporting).
 */
class ZemoteApp : Application()
